using System.Runtime.CompilerServices;

namespace Artemis.Plugins.Games.TruckSimulator.Telemetry;

[InlineArray(2)]
public struct Array2<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(3)]
public struct Array3<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(8)]
public struct Array8<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(10)]
public struct Array10<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(16)]
public struct Array16<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(20)]
public struct Array20<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(24)]
public struct Array24<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(25)]
public struct Array25<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(28)]
public struct Array28<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(32)]
public struct Array32<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(48)]
public struct Array48<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(52)]
public struct Array52<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(56)]
public struct Array56<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(60)]
public struct Array60<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(64)]
public struct Array64<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(90)]
public struct Array90<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(152)]
public struct Array152<T> where T : unmanaged
{
    private T _t;
}

[InlineArray(192)]
public struct Array192<T> where T : unmanaged
{
    private T _t;
}
